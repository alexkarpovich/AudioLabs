'use strict';

var React = require('react');

var StageItem = React.createClass({
    render: function() {
        return (
            <ul className="stage-item">StageItem</ul>
        );
    }
});

module.exports = StageItem;