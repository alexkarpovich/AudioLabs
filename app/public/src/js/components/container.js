var React = require('react');

var Container = React.createClass({
    render: function() {
        return (
            <div>
                Container component
            </div>
        );
    }
});

module.exports = Container;