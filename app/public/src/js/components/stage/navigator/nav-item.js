'use strict';

var React = require('react');

var NavItem = React.createClass({
    render: function() {
        return (
            <ul className="nav-item">StageItem</ul>
        );
    }
});

module.exports = NavItem;